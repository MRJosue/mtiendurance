<div

>
    {{-- <div @click.outside="show = false" class="bg-white rounded-lg shadow-lg w-full max-w-lg relative p-6">
        <button @click="show = false"
                class="absolute top-2 right-2 text-gray-500 hover:text-gray-700 text-xl">
            &times;
        </button>

        <div class="mb-4">
            <h2 class="text-lg font-semibold">{{ $title ?? 'Modal' }}</h2>
        </div>

        <div class="mb-4">
            {{ $slot }}
        </div>
    </div> --}}
</div>
